// Ambient module declarations for dependencies that don't ship their own types
// or are only available at build time (not to npm consumers).
declare module "@turf/turf";
declare module "polygon-clipping";
