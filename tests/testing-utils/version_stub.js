// Test stub for the ESM `src/version.js` module.
// The real file uses ESM `export` syntax which jest does not transform, and its value is
// irrelevant to unit tests. See jest.config.js `moduleNameMapper`.
module.exports = { ULABEL_VERSION: "0.0.0-test" };
